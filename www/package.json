{
  "name": "@frankenpenguin/www",
  "version": "0.0.1",
  "description": "",
  "author": "tauseefk",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "node build.js",
    "lint": "biome check src",
    "dev": "NODE_ENV=development nodemon --ext 'js,wasm' --watch src build.js"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "esbuild-plugin-copy": "^2.1.1",
    "esbuild-plugin-wasm": "^1.1.0",
    "nodemon": "^3.1.9",
    "npm-run-all": "^4.1.5"
  }
}
